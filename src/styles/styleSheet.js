import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
      flex: 1,
    },

    input: {
      height:40,
      margin: 12,
      borderWidth: 1,
      padding: 30,
    },

    login: {
      color: 'blue',
      fontSize: 50,
      bottom: 150,
    },

    image: {
      height: '100%',
      width: '100%',
      alignItems: 'center',
      justifyContent: 'center',
      height:'100%',
    },

    cavalo: {
      width: 200,
      height:200,
    }
  });